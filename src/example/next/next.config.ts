import type { NextConfig } from 'next';
import memoryRuntimeIntelligence from 'memory-runtime-intelligence/next';

const nextConfig: NextConfig = {};

export default memoryRuntimeIntelligence(nextConfig);
